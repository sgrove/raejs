// TODO: Add multiple environment support

config = jZenFn({
    standard_name: "Config",
    description:   "Holds all the environment-specific configurations",
    loggerLevel: 90,
    database: "wtf_shopping"
});

exports.config = config;
